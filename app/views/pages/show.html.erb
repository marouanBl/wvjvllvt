<p id="notice"><%= notice %></p>

<p>
  <b>Number:</b>
  <%= @page.number %>
</p>

<p>
  <b>Issue:</b>
  <%= @page.issue_id %>
</p>

<p>
  <b><%= link_to @page.template.name, edit_template_path(@page.template_id) %></b>
  <div>
  	<%= @page.template.content.html_safe %>
  </div>

</p>


<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
